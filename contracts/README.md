# Contracts

API and data contracts. These are the source of truth for how frontend and backend communicate.

Create contracts using `templates/contract.md`.

Rules:
- Contracts must exist before implementation tasks that consume them
- Frontend and backend both consume contracts — neither invents their own
- Changes to contracts require review
